(function() {
/**
 * Personal Hub — FAB 已移除（功能合并到 TabBar 中心➕按钮）
 * 保留空壳以避免 index.html script 引用报错
 */

const { createElement: h } = React;

function FAB() {
  // 不再渲染任何内容
  return null;
}

window.FAB = FAB;

})();
